---
title: "{{TITLE}}"
slug: "{{SLUG}}"
generatedAt: "{{ISO}}"
goal: "{{GOAL}}"
status: draft
stack: {{STACK}}
---

import {
  PlanHeader, PlanSidebar, PlanStep, Mermaid,
  DecisionForm, Decision, CopyDecisions, ProgressBar
} from "./components.js";

<style>{`
  :root {
    --bg: #fafafa;
    --bg-elev: #ffffff;
    --fg: #0f172a;
    --muted: #475569;
    --accent: #4f46e5;
    --accent-soft: #eef2ff;
    --border: #e2e8f0;
    --good: #16a34a;
    --warn: #d97706;
    --bad:  #dc2626;
  }
  @media (prefers-color-scheme: dark) {
    :root {
      --bg: #0b1020;
      --bg-elev: #111827;
      --fg: #e2e8f0;
      --muted: #94a3b8;
      --accent: #818cf8;
      --accent-soft: #1e1b4b;
      --border: #1f2937;
    }
  }
  html, body { background: var(--bg); color: var(--fg); font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, sans-serif; }
  .plan-grid { display: grid; grid-template-columns: 280px 1fr; gap: 24px; max-width: 1280px; margin: 0 auto; padding: 24px; }
  @media (max-width: 768px) { .plan-grid { grid-template-columns: 1fr; } }
  .plan-sidebar { position: sticky; top: 24px; align-self: start; height: calc(100vh - 48px); overflow-y: auto; padding: 16px; background: var(--bg-elev); border: 1px solid var(--border); border-radius: 12px; }
  .plan-main { min-width: 0; }
  .plan-card { background: var(--bg-elev); border: 1px solid var(--border); border-radius: 12px; padding: 24px; margin-bottom: 20px; }
  .plan-step-num { display: inline-flex; align-items: center; justify-content: center; width: 32px; height: 32px; border-radius: 9999px; background: var(--accent); color: white; font-weight: 700; }
  .plan-chip { display: inline-block; padding: 2px 10px; border-radius: 9999px; background: var(--accent-soft); color: var(--accent); font-size: 12px; font-weight: 600; margin-right: 6px; }
  .plan-step-link { padding: 6px 10px; border-radius: 8px; display: block; color: var(--muted); text-decoration: none; font-size: 14px; }
  .plan-step-link.active { background: var(--accent-soft); color: var(--accent); font-weight: 600; }
  .plan-progress { position: fixed; top: 0; left: 0; right: 0; height: 4px; background: var(--border); z-index: 50; }
  .plan-progress > div { height: 100%; background: var(--accent); transition: width 0.3s ease; }
  details.decision { border: 1px solid var(--border); border-radius: 12px; padding: 16px; margin-bottom: 12px; background: var(--bg-elev); }
  details.decision[open] { box-shadow: 0 0 0 2px var(--accent-soft); }
  details.decision > summary { cursor: pointer; font-weight: 600; }
  .radio-row { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 12px; }
  .radio-row label { display: flex; flex-direction: column; padding: 10px 14px; border: 1px solid var(--border); border-radius: 10px; cursor: pointer; flex: 1 1 180px; transition: all 0.15s; }
  .radio-row label:hover { border-color: var(--accent); }
  .radio-row input { display: none; }
  .radio-row input:checked + .radio-content { color: var(--accent); }
  .radio-row label:has(input:checked) { border-color: var(--accent); background: var(--accent-soft); }
  .radio-title { font-weight: 600; }
  .radio-help  { font-size: 12px; color: var(--muted); margin-top: 4px; }
  textarea.decision-note { width: 100%; margin-top: 12px; padding: 8px 10px; border: 1px solid var(--border); border-radius: 8px; background: var(--bg); color: var(--fg); font-family: inherit; font-size: 13px; min-height: 60px; }
  .copy-bar { position: sticky; bottom: 16px; display: flex; gap: 12px; justify-content: flex-end; padding: 12px; background: var(--bg-elev); border: 1px solid var(--border); border-radius: 12px; margin-top: 16px; }
  .copy-bar button { padding: 10px 18px; border-radius: 10px; border: none; background: var(--accent); color: white; font-weight: 600; cursor: pointer; }
  .copy-bar button.secondary { background: transparent; color: var(--accent); border: 1px solid var(--accent); }
  pre.mermaid-fallback { background: #0f172a; color: #e2e8f0; padding: 16px; border-radius: 8px; overflow-x: auto; font-size: 13px; }
  .decision-preview { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 12px; background: var(--bg); border: 1px solid var(--border); border-radius: 8px; padding: 12px; max-height: 280px; overflow-y: auto; }
`}</style>

<div className="plan-progress"><div id="plan-progress-bar" style="width:0%"></div></div>

<PlanHeader
  title="{{TITLE}}"
  goal="{{GOAL}}"
  generatedAt="{{ISO}}"
  stack={ {{STACK}} }
  status="draft"
/>

<div className="plan-grid">

<aside className="plan-sidebar">
  <ProgressBar />
  <PlanSidebar steps={[
    { id: "overview", title: "Overview" },
    {{STEPS_SIDEBAR}}
    { id: "decisions", title: "Decisions" },
  ]} />
</aside>

<main className="plan-main">

<section id="overview" className="plan-card">
  <h2>Overview</h2>
  <p>{{OVERVIEW_TEXT}}</p>
  <h3>Goals</h3>
  <ul>{{GOALS_LIST}}</ul>
  <h3>Non-Goals</h3>
  <ul>{{NON_GOALS_LIST}}</ul>
</section>

{{STEPS}}

<section id="decisions" className="plan-card">
  <h2>Decisions needed before implementation</h2>
  <p>Pick an option for each question, add notes if needed, then copy the block at the bottom and paste it back to the agent.</p>

  <DecisionForm id="plan-decisions">
    {{DECISIONS}}
  </DecisionForm>

  <CopyDecisions target="#plan-decisions" />

  <hr style="margin: 24px 0; border: 0; border-top: 1px solid var(--border);" />

  <h3>How to use this file</h3>
  <ul>
    <li>Decisions stay in your browser — nothing is sent anywhere.</li>
    <li>Click <strong>Copy decisions</strong> at the bottom to get a paste-ready block.</li>
    <li>Paste it back into the chat and the agent will implement the plan with your choices.</li>
  </ul>
</section>

</main>
</div>

<script type="module">{`
  import mermaid from "https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.esm.min.mjs";
  mermaid.initialize({ startOnLoad: true, theme: "neutral", securityLevel: "loose", fontFamily: "inherit" });
  // Re-render after a tick (in case of late DOM insertion)
  setTimeout(() => mermaid.run({ querySelector: ".mermaid" }), 50);
`}</script>